spring:
  datasource:
    url: ${JDBC_URL:jdbc:postgresql://192.168.1.53:5432/vocabulary}
    username: ${DB_USER:vocabulary}
    password: ${DB_PASSWORD:vocabulary}
    driver-class-name: org.postgresql.Driver