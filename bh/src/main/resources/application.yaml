spring:
  datasource:
    url: ${JDBC_URL:jdbc:postgresql://localhost/vocabulary}
    username: ${DB_USER:vocabulary}
    password: ${DB_PASSWORD:vocabulary}
    driver-class-name: org.postgresql.Driver

telegram:
  botName: ${BOT_NAME}
  token: ${BOT_TOKEN}
